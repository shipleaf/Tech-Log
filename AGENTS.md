# AGENTS.md

> Codex 작업 실행 규칙의 단일 진실 공급원입니다.
> Codex는 모든 태스크에서 이 문서를 먼저 읽고, 즉시 [`CONTEXT_MAP.md`](./CONTEXT_MAP.md)를 이어서 로드합니다.

---

## 적용 범위

- 코드, 설정, CI, 테스트, 문서 변경을 포함한 모든 작업에 적용한다.
- 예외는 명시적으로 문서화된 경우에만 허용한다.

---

## 필수 시작 순서

1. `AGENTS.md`를 읽는다.
2. [`CONTEXT_MAP.md`](./CONTEXT_MAP.md)를 읽는다.
3. 관련 제품 명세, 아키텍처, 활성 실행 계획, 품질 문서를 로드한다.
4. 작업 범위, 변경 대상, 검증 방법, 완료 조건이 있는 작은 단위 플랜을 먼저 만든다.
5. 만든 플랜을 즉시 `docs/exec-plans/active/`에 기록한다.
6. 전용 `worktree`와 작업 브랜치를 준비한 뒤 작업을 시작한다.

위 순서를 마치기 전에는 파일 수정, 테스트 작성, 검증, 커밋, 완료 보고를 시작하지 않는다.

---

## 작업 실행 규칙

### 1. 플랜 선행

- 모든 작업은 반드시 플랜을 먼저 만든 뒤 시작한다.
- 플랜에는 범위, 변경 대상, 검증 방법, 종료 조건이 포함되어야 한다.
- 플랜은 반드시 하나의 명확한 목적만 다루는 작은 단위여야 한다.
- 큰 목표나 마일스톤은 `docs/PLANS.md`에서 추적하고, 실제 실행 단위는 `docs/exec-plans/active/`에 기록한다.
- 플랜을 세운 직후 `docs/exec-plans/active/`에 문서로 남기기 전에는 구현을 시작하지 않는다.
- 계획 없이 구현을 시작하지 않는다.

### 2. `worktree`에서 작업

- 모든 구현과 수정은 반드시 분리된 `worktree`에서 수행한다.
- `main`, `master`, 기본 작업 디렉터리에서 직접 구현하지 않는다.
- 어떤 브랜치와 `worktree`에서 작업 중인지 식별 가능한 상태여야 한다.

### 3. 테스트 코드 작성 필수

- 코드나 동작을 변경하는 작업에는 테스트 코드를 반드시 작성한다.
- 기존 동작을 바꾸면 기존 테스트를 함께 갱신한다.
- 문서 전용 변경은 자동화 테스트 대상이 아니지만, 링크와 내용 정합성 검증은 필수다.

### 4. 검증 필수

- 완료 전 반드시 관련 검증을 수행한다.
- 기본 검증 세트는 `빌드 → 린트 → 테스트`이며, 작업 특성에 맞는 추가 검증을 포함한다.
- 검증이 실패하면 완료 보고 대신 원인 수정 또는 하네스 보완을 우선한다.

### 5. 종료 흐름 고정

- 모든 작업은 `플랜 수립 → active 기록 → worktree 준비 → 구현 → 테스트 작성 → 검증 → completed 이동 → 커밋 → 머지 → 완료 보고`의 순서를 따른다.
- 커밋 전에 검증이 끝나야 하며, 머지 전에는 테스트가 빠지면 안 된다.
- 실행 계획 문서는 작업 검증이 끝나면 `completed/`로 이동한다.
- Codex는 종료 단계에서 머지를 직접 수행한다. 기본 하네스 경로는 `./harness.sh ship <task-id> <commit-message> develop`이며, 동등한 절차를 수동으로 나누어 실행하더라도 최종 상태는 반드시 `develop` 반영까지 포함해야 한다.
- 완료 보고는 커밋과 머지까지 끝난 뒤에만 한다. 머지 권한이나 환경이 없으면 그 상태를 명확히 남긴다.

---

## 하네스 원칙

- 사람이 기억해서 지키는 규칙보다 하네스가 강제하는 규칙을 우선한다.
- 규칙 위반이 반복되면 "더 조심한다"가 아니라 하네스를 수정한다.
- 플랜 부재, `worktree` 미사용, 테스트 누락, 검증 누락, 머지 전 완료 보고는 하네스가 차단해야 한다.

---

_마지막 갱신: 2026-04-15 | 검증 상태: 초안_
